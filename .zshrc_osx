#oh-my-zsh
ZSH=$HOME/.oh-my-zsh
DEFAULT_USER="jake"
ZSH_THEME="clean"
# (random) clean miloshadzic takashiyoshida sporty_256 sorin robbyrussell nicoulaj
source $ZSH/oh-my-zsh.sh

# tmux on start with zsh
if [ "$TMUX" = "" ]; then tmux; fi

#Aliases
alias caffeine="echo 'No Sleep For an HOUR!'; caffeinate -u -t 3600"
alias andsdk="~/Code/SDKs\ -\ Libraries/android-sdk/tools/android sdk"
alias site-install="~/Code/Web/Scripts/install/main_install.sh"
alias gob="go build"
alias bupdate='brew update && brew upgrade && brew cleanup'
alias git="hub"
alias sudo="nocorrect sudo"

# Suffix Aliases - open filetypes with program of choice
alias -s html=mvim
alias -s css=mvim
alias -s js=mvim
alias -s php=mvim
alias -s erb=mvim
alias -s c=mvim
alias -s cpp=mvim
alias -s go=mvim

#oh-my-zsh-plugins
plugins=(git github vundle rbenv ruby rails rails3 brew encode64 osx tmux golang)

# Miscellaneous functions
source $HOME/.zsh_aliases

# if GVM installed
source $HOME/.gvm/scripts/gvm
# RBENV
eval "$(rbenv init -)"

#HOMEBREW, DRUSH, NPM Binaries
#DEFAULT PATH=/usr/bin:/bin:/usr/sbin:/sbin:/usr/local/bin
export PATH=/usr/local/bin:$PATH:/usr/local/sbin
export PATH="$(brew --prefix josegonzalez/php/php54)/bin:$PATH"
export PATH=/usr/local/share/npm/bin:$PATH
